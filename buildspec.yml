version: 0.2
phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
  build:
    commands:
      - echo Build started on `date`
      - echo Building the JAR file
      - mvn clean package -DskipTests
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/Core-Server-0.0.1-SNAPSHOT.jar
    - Procfile
    - .ebextensions/**/*
  name: crt-portal-server
