# Production Configuration
server.port=5000

# Database Configuration
spring.datasource.url=jdbc:mysql://springboot-aurora-cluster.cluster-cyfawggm26cp.us-east-1.rds.amazonaws.com:3306/springbootdb
spring.datasource.username=admin
spring.datasource.password=SpringBootDB2024!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Logging
logging.level.com.crt.server=INFO
logging.level.org.springframework.security=WARN

# CORS Configuration
cors.allowed.origins=https://your-frontend-domain.com

# Redis Configuration (if using external Redis)
# spring.redis.host=your-redis-cluster.cache.amazonaws.com
# spring.redis.port=6379

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:}
spring.mail.password=${EMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Client URL
client.url=https://your-frontend-domain.com
